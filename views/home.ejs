<%- include("partials/header"); -%>

<h1>Hello</h1>
<p><%= homeContent %></p>

<%for(let i=0;i < posts.length ; i++){ %>
   <h1><%= posts[i].title %> <a href="delete/<%= posts[i]._id %>" class="btn btn-dark btn-default btn-circle"><i class="fa fa-user"></i></a></h1>

  <% if(posts[i].content.length > 100){ %>
       <p><%= posts[i].content.substring(0,100) + " ..."%>
           <a href="posts/<%= posts[i]._id %>">Read More</a>
       </p>
  <% }else{  %>
         <p><%= posts[i].content %></p>
   <%  } %>
<% } %>

<%- include("partials/footer"); -%>